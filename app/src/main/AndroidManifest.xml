<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.zanpo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Splash"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".NavigationView.SettingsActivity"
            android:parentActivityName=".MainScreen.ApiActivity" />
        <activity
            android:name=".NavigationView.FavoritesActivity"
            android:parentActivityName=".MainScreen.ApiActivity" />
        <activity
            android:name=".NavigationView.AccountActivity"
            android:parentActivityName=".MainScreen.ApiActivity" />
        <activity
            android:name=".SignUpLogin.SignUpLoginActivity" />
        <activity
            android:name=".SignUpLogin.LoginActivity"
            android:parentActivityName=".SignUpLogin.SignUpLoginActivity" />
        <activity
            android:name=".SignUpLogin.SignUpActivity"
            android:parentActivityName=".SignUpLogin.SignUpLoginActivity" />
        <activity
            android:name=".MainScreen.ApiActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>